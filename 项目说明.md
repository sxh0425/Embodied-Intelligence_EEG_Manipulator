# Embodied-Intelligence_EEG_Manipulator

脑机接口（EEG）× 具身智能（机械臂）的学习与实验仓库。

**目标链路**：EEG 意图解码 → 目标选择 → 机械臂自主抓取（仿真）

> ## 👉 不知道从哪开始？看 `从这里开始.md`
> **那是唯一的入口。** 下面的内容等你走完那一页再看。

## 当前进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0a | 环境搭建（conda + MuJoCo + Panda 模型） | ✅ 完成 |
| 0b | MuJoCo 入门（读 model / data / 位姿 / 转轴 / 积分） | ✅ 完成（热身 12/12） |
| 0c | 运动学练习 EX1~EX7 | 🔵 **进行中** |
| 1 | 抓取与操作（pick-and-place） | ⬜ 待展开 |
| 2 | 具身智能算法（LeRobot：ACT / Diffusion Policy） | ⬜ 待展开 |
| 3 | EEG / 脑机接口解码 | ⬜ 待展开 |
| 4 | 融合闭环（毕设主线） | ⬜ 待展开 |

## 目录结构

```
从这里开始.md    唯一入口。不知道做什么就看它
项目说明.md      本文件：仓库总览
路线图.md        8 个月的全景路线与选型（平时不用看）

课程/            线性课程，编号 = 关卡编号，按顺序读
  ├── 目录.md                      课程目录 + 进度表
  ├── 第00课-先看懂代码里的Python.md
  ├── 第04课 ~ 第12课              对应热身第 4~12 级
  └── 速查-MuJoCo字段对照表.md      字典，需要时查，不要通读

robotics/        代码，按数字顺序做
  ├── 01_load_panda.py    读模型、打印基本信息
  ├── 02_view_panda.py    打开可视化窗口
  ├── 03_experiments.py   预测题（先猜再看结果）
  ├── 04_warmup.py        热身关卡第 0~12 级 ✅ 已完成
  └── 05_exercises.py     正式练习 EX1~EX7 ← 现在做这个

assets/          模型文件（不进版本库）
```

`learning/`（Stage 4 策略训练）、`bci/`（EEG 解码）等目录**等到那一步再建**，现在留空。

## 学习顺序

```
01_load_panda.py → 02_view_panda.py → 03_experiments.py
    → 04_warmup.py（12 级热身，每级只加一个新东西）
        → 05_exercises.py（EX1~EX7，带自动检查）
```

**不要跳步。** 直接做 `05_exercises.py` 会卡住——它从 EX4 开始一道题要用 5 个没见过的 API。

## 硬件分工

本机是 2018 款 Intel MacBook Pro（4 核 i5 / 8 GB 内存 / 无独显）：

| 在哪 | 干什么 |
|---|---|
| **Mac（本地）** | 写代码、MuJoCo 仿真、看可视化窗口、小规模推理 |
| **AutoDL / 实验室服务器** | 只跑训练（策略网络、VLA 微调）——现在还用不到 |
| **连真机的电脑** | 采集机械臂数据、真机推理（有真机之后） |

**关键限制**：这台 Mac 装不了 LeRobot（最低要求 `torchvision>=0.21`，Intel macOS 上最高只到 0.17.2）。所以 LeRobot 相关的一切都在服务器上做。

## 环境

```bash
conda activate eeg-arm      # 每次开新终端都要先跑这句
```

已有的环境（Python 3.11.13）：`mujoco 3.10.0`、`numpy`、`scipy`、`matplotlib`、`gymnasium`。

重建环境：

```bash
conda create -n eeg-arm python=3.11 -y
conda activate eeg-arm
pip install "mujoco<3.11" numpy scipy matplotlib gymnasium -i https://pypi.tuna.tsinghua.edu.cn/simple
```

（`mujoco` 必须小于 3.11——3.11 起官方不再发布 Intel macOS 的预编译包。）

模型已经在 `assets/mujoco_menagerie/franka_emika_panda/`，不用重新下载。

