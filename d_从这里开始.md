# 从这里开始

> **如果你不知道看哪个文件、该做什么 —— 只看这一页，其他全都不看。**

---

## 先说清楚

这个仓库现在有 12 个文档、2500 多行。**这是我的失误。**
每次你问一个小问题，我都回你一份 300 行的新文档 —— 结果就是你被淹没了。

**现在请把其他文件全部忘掉。** 这一页就够了。

---

## 你今天要做的，只有三件事

### ① 读完这一页（你正在做）

### ② 只读这一份文档的 3 个小节

打开 `docs/教案/01-MuJoCo是什么.md`，**只读这三节**：

- 第 0 节（MuJoCo 是什么）
- 第 2 节（物理引擎的四步循环）
- 第 4 节（状态为什么是一个数组）

**一次读完，不要记笔记，不要做练习。** 读完就来第 ③ 步。

### ③ 打开 `robotics/warmup.py`，只做第 3 级

找到第 3 级里这两行：

```python
    ######## TODO ########
    return None
```

把它们改成：

```python
    mujoco.mj_forward(model, data)
    return data.xpos.shape
```

然后运行：

```bash
conda activate eeg-arm
python robotics/warmup.py
```

**看到第 3 级从 ⬜ 变成 ✅，今天就到此为止。**

---

## 如果第 3 级你也看不懂 —— 看这里

把要改的那一行拆开：

```python
    return data.xpos.shape
```

| 部分 | 什么意思 |
|---|---|
| `data` | 一个"盒子"，装着这次仿真的**所有状态** |
| `.xpos` | 盒子里的一个**格子**，装着"每个刚体此刻在哪里" |
| `.shape` | 问这个格子：**"你里面有几个数？是怎么排的？"** |
| `return` | 把这个答案**交出去**，作为这个函数的结果 |

另外要改的第一行 `mujoco.mj_forward(model, data)` 是**必须的**：不先算一次，`.xpos` 里面是空的。

跑完你会看到 `(12, 3)`。意思是：

> **12 组数，每组 3 个数。**
> 12 = 模型里有 12 个刚体；3 = 每个刚体的位置用 3 个数表示（x, y, z）。

**如果连这个都还是不懂 —— 直接告诉我"第 3 级里 `.shape` 是什么意思"，我就只讲这一个点。**

---

## 这一周不要碰的文件

| 文件 | 为什么现在不要看 |
|---|---|
| `docs/学习路径总览.md` | 那是 8 个月的全景图。现在看只会让你焦虑 |
| `docs/教案/02-可视化原理.md` 及以后的 | 顺序还没到 |
| `docs/教案/05-*.md` | 概念讨论，等你有手感了再看 |
| `docs/速查/MuJoCo字段对照表.md` | 这是**字典**，需要时查一个字段，不要通读 |
| `docs/笔记/01-MuJoCo心智模型.md` | 教案的浓缩版，重复内容 |
| `robotics/exercises.py` | 等 warmup 12 级全过了再来 |
| `robotics/03_experiments.py` | 同上 |

**只碰这两个文件**：`docs/教案/01-MuJoCo是什么.md` 和 `robotics/warmup.py`。

---

## 每天 20 分钟，只做一级

```
每天打开 warmup.py
   ↓
只做完一级（通常只有 1~3 行代码）
   ↓
关掉，明天再来
```

12 级 = 12 天。**不要一天做三级**，那会变成抄代码。

**现在你已经完成到第 2 级了，今天做第 3 级。**

---

## 卡住的时候怎么问我

**❌ 这样说，我帮不了你：**
> 「我不懂」「太难了」

**✅ 这样说，我能精确回答：**
> 「第 3 级里，`.shape` 是干什么的？为什么加了这个就能通过？」
> 「第 4 级里 `mj_name2id` 那三个参数分别是什么？」

**告诉我具体哪一行、哪个词不懂。** 我就只讲那一个点，不再给你新文档。

---

## 就这些

其他一切，等你把 warmup 12 级全过了再说。
