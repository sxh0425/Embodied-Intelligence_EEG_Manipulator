# 从这里开始

只有这一条路，不要乱翻别的文件：

1. 打开 `课程/目录.md`
2. 读**今天的那一课**（一次只读一课）
3. 打开 `robotics/warmup.py`，把对应的那一级写完
4. 运行 `python robotics/warmup.py`，看到 ✅ 就收工

跑代码之前，先在终端里激活环境（每次开新终端都要）：

```bash
conda activate eeg-arm
```

`docs/` 目录里的所有 md 都是旧的、散的文件，**不用看**。

